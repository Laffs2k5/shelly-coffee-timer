# Android Studio
app/.gradle/
app/build/
app/local.properties
app/.idea/
app/*.iml
app/captures/
app/.externalNativeBuild/

# Credentials — never commit these
*.env
secrets.*

# Private local-only dir for secrets that don't fit a single .env file
# (e.g. local MQTT client certs + creds, cloud MQTT creds — different sets).
# Anything under private/ is ignored. Keep ALL real keys/certs here.
private/

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
